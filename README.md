# Generate-MD5
This script is aimed to gengrate MD5 checksum to verify the integrity of files.

You need to get into the code to change the file path to the file you want to check.  
e.g. 
```python
f = open("/home/file.tar", "rb")
```